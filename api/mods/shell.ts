import { Response } from "../../utils/types.ts";

export async function interaction_shell(path:string, method:string, headers:Headers, body:any) {
    let response:Response = {}



    return response;
}